{
  "name": "portfolio1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf rm dist/*",
    "dev:client": "npm run clean && parcel src/views/*.pug --open --port 3000 --no-cache",
    "dev:server": "nodemon src/server.js",
    "dev": "parcel src/views/*.pug --no-cache && nodemon src/server.js",
    "build-client": "npm run clean && parcel build src/views/*.pug --no-source-maps --detailed-report --out-dir dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.0.0",
    "gh-pages": "^2.1.1",
    "nodemon": "^1.19.3",
    "parcel-bundler": "^1.12.4",
    "pug": "^2.0.4",
    "rimraf": "^3.0.0",
    "sass": "^1.22.10"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "http-status": "^1.4.0",
    "mongoose": "^5.7.4"
  }
}
